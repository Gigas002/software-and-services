# packages

## scoop

Export via:

```sh
scoop export packages.json
```

Import via:

```sh
scoop import packages.json
```

## winget

Export via:

```sh
winget export -o packages.json
```

Import via:

```sh
winget import -i packages.json
```
